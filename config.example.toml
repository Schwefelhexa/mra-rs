[pairs.personal.source]
type = "imap"
host_cmd = "op item get \"Personal Mail\" --fields host"
port = 993
username = "test"
password = "test"

[pairs.personal.destination]
type = "maildir"
path = "./Maildir/personal"  # TODO: support ~

